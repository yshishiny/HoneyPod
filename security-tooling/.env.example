# ============================================================================
# HoneyPod SIEM Stack - Environment Variables
# Copy this file to .env and set your own values before running docker-compose
# NEVER commit .env to version control
# ============================================================================

# Redis password (used by Logstash pipeline and Redis container)
REDIS_PASSWORD=honeypod-redis-password-2024
